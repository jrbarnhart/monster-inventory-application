extends layout 

block content 
  div.monster-list-content
    h2 All Monsters 

    ul
      each monster in monster_list 
        li 
          p #[a.name(href=monster.url) #{monster.name}] - #[a.family(href=monster.family.url) #{monster.family.name}] Family
      else 
        p There are no monsters.